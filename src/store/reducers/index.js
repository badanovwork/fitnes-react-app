
const fitnerApp = (state = [1,2,3], action) => {
    switch (action.type) {
        case 'ADD_TODO':
            return state;
        default:
            return state;
    }
}

export default fitnerApp